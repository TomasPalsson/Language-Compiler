#[derive(Debug, PartialEq, Clone)]
pub enum Token {
    Number(i64),
    Identifier(String),
    StringLiteral(String),
    Function,
    FunctionCall(String),
    If,
    While,
    Do,
    Then,
    Else,       
    Print,
    Send,
    Fetch,
    Plus,
    Eq,
    NotEq,
    Greater,
    Less,
    LessEq,
    Minus,
    Multiply,
    Divide,
    Assign,
    LParen,
    RParen,
    Comma,
    Semicolon,
    End,
}

